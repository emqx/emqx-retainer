## Retainer config schema

storage_type {
  datatype = enum
  enums = [ram, disc, disc_only]
}

max_retained_messages {
  datatype = integer
}

max_payload_size {
  datatype = bytesize
}

expiry_interval {
  datatype = duration
}
